# Task 17 Evidence: tailscale/derp Build Status

## Build Verification

### tailscale protocol package
```
$ go build -tags 'with_gvisor,with_quic,with_dhcp,with_wireguard,with_utls,with_acme,with_clash_api,with_tailscale,with_ccm,with_ocm' ./protocol/tailscale
EXIT_CODE: 0 (SUCCESS)
```

### derp service package
```
$ go build -tags 'with_gvisor,with_quic,with_dhcp,with_wireguard,with_utls,with_acme,with_clash_api,with_tailscale,with_ccm,with_ocm' ./service/derp
EXIT_CODE: 0 (SUCCESS)
```

### Combined build
```
$ go build -tags 'with_gvisor,with_quic,with_dhcp,with_wireguard,with_utls,with_acme,with_clash_api,with_tailscale,with_ccm,with_ocm' ./protocol/tailscale ./service/derp
EXIT_CODE: 0 (SUCCESS)
```

## Symbol/API Analysis

### sing-tun v0.8.9 API Status

The code in `protocol/tailscale/endpoint.go` references these symbols from `github.com/sagernet/sing-tun`:

| Line | Symbol | Status in v0.8.9 |
|------|--------|------------------|
| 90 | `tun.ICMPForwarder` | **PRESENT** - type alias in tun package |
| 308 | `tun.CalculateInterfaceName` | **PRESENT** |
| 310 | `tun.Options` | **PRESENT** |
| 320 | `tun.New` | **PRESENT** |
| 383 | `tun.DefaultNIC` | **PRESENT** - const = 1 |
| 387 | `tun.DefaultNIC` | **PRESENT** - const = 1 |
| 391 | `tun.NewICMPForwarder` | **PRESENT** - function in tun package |
| 634 | `tun.DirectRouteContext` | **PRESENT** |
| 655 | `tun.ErrDrop` | **PRESENT** |
| 727 | `tun.DirectRouteDestination` | **PRESENT** |

### ping package API Status

The code in `protocol/tailscale/endpoint.go` references from `github.com/sagernet/sing-tun/ping`:

| Line | Symbol | Status in v0.8.9 |
|------|--------|------------------|
| 732 | `ping.ConnectDestination` | **PRESENT** |
| 742 | `ping.ConnectGVisor` | **PRESENT** |

### wireguard transport (parallel usage)

The wireguard transport (`transport/wireguard/device_system_stack.go`, `transport/wireguard/device_stack.go`) uses the same sing-tun APIs:
- `tun.DefaultNIC` - used at lines 72, 85, 104, 109, 145, 149
- `tun.NewICMPForwarder` - used at lines 80, 94
- `ping.ConnectGVisor` - used at line 165

These compile successfully, confirming the API is available.

## CURRENT_ISSUES.md Classification

The `CURRENT_ISSUES.md` Issue 1.3 claims these symbols are undefined:
```
protocol/tailscale/endpoint.go:87:25: undefined: tun.ICMPForwarder
protocol/tailscale/endpoint.go:348:34: undefined: tun.DefaultNIC
protocol/tailscale/endpoint.go:356:23: undefined: tun.NewICMPForwarder
protocol/tailscale/endpoint.go:685:27: undefined: ping.ConnectGVisor
```

**VERIFICATION**: These line numbers (87, 348, 356, 685) do NOT match the current file content. The actual usages are at:
- Line 90: `icmpForwarder *tun.ICMPForwarder`
- Line 308: `tun.CalculateInterfaceName`
- Line 383: `tun.DefaultNIC`
- Line 391: `tun.NewICMPForwarder`
- Line 742: `ping.ConnectGVisor`

**CONCLUSION**: Issue 1.3 is STALE - it references old line numbers and the symbols ARE available in sing-tun v0.8.9.

## Build Constraint Analysis

The tailscale/derp packages use `//go:build with_gvisor` constraints:
- `protocol/tailscale/endpoint.go`: `//go:build with_gvisor`
- `protocol/tailscale/tun_device_unix.go`: `//go:build with_gvisor && !windows`
- `protocol/tailscale/tun_device_windows.go`: `//go:build with_gvisor && windows`
- `protocol/tailscale/dns_transport.go`: `//go:build with_gvisor`
- `service/derp/service.go`: `//go:build with_gvisor`

Without the `with_gvisor` tag, Go reports "build constraints exclude all Go files".

The default build tags (from `release/DEFAULT_BUILD_TAGS_OTHERS`) include `with_gvisor` and `with_tailscale`, so a standard release build will include these packages.

## Overall Status

| Package | Without Tags | With Tags | With Full Tags |
|---------|-------------|-----------|---------------|
| protocol/tailscale | EXCLUDED | EXCLUDED | EXCLUDED (needs with_gvisor) |
| protocol/tailscale | N/A | BUILD SUCCESS (with_gvisor) | BUILD SUCCESS |
| service/derp | EXCLUDED | EXCLUDED | EXCLUDED (needs with_gvisor) |
| service/derp | N/A | BUILD SUCCESS (with_gvisor) | BUILD SUCCESS |

**FINAL VERDICT**: No build failures for tailscale/derp surfaces when built with appropriate build tags (`with_gvisor`, `with_tailscale`). The issues documented in CURRENT_ISSUES.md are stale/incorrect.

## Classification

- **Pre-existing issue 1.3 status**: STALE - symbols are available
- **Not caused by upstream merge**: The sing-tun v0.8.9 API is compatible
- **No fix needed**: Build succeeds with proper tags
- **Remaining issue**: quic-go HTTP/3 dependency mismatch (separate issue, not tailscale/derp)
